From 1a9ada5cd2caef9d092900351f21831c8914ad99 Mon Sep 17 00:00:00 2001
From: Kubmichna <jmichna64@gmail.com>
Date: Sat, 6 Jun 2026 20:50:05 +0200
Subject: [PATCH] fix(perf): replace lock-based TpsTracker with atomic ring
 buffer, eliminate per-packet registry allocation and session lock contention
 in relay hot path

---
 crates/proxy-core/src/features/commands.rs    |   8 +-
 crates/proxy-core/src/net/packet_builder.rs   |   4 +-
 crates/proxy-core/src/net/relay.rs            |  27 +-
 crates/proxy-core/src/proxy.rs                |  70 +++--
 ...ning-graceful-shutdown-handshake-kee.patch | 243 ++++++++++++++++++
 5 files changed, 307 insertions(+), 45 deletions(-)
 create mode 100644 patches/0001-feat-proxy-hardening-graceful-shutdown-handshake-kee.patch

diff --git a/crates/proxy-core/src/features/commands.rs b/crates/proxy-core/src/features/commands.rs
index 028d341..58e9679 100644
--- a/crates/proxy-core/src/features/commands.rs
+++ b/crates/proxy-core/src/features/commands.rs
@@ -302,10 +302,10 @@ async fn handle_gtps(
 ) -> CommandResult {
     send_message("§6§lServer TPS (Ticks Per Second)".to_owned());
 
-    let tps_5s = state.tps_tracker.calculate_tps(5).await;
-    let tps_10s = state.tps_tracker.calculate_tps(10).await;
-    let tps_20s = state.tps_tracker.calculate_tps(20).await;
-    let tps_30s = state.tps_tracker.calculate_tps(30).await;
+    let tps_5s = state.tps_tracker.calculate_tps(5);
+    let tps_10s = state.tps_tracker.calculate_tps(10);
+    let tps_20s = state.tps_tracker.calculate_tps(20);
+    let tps_30s = state.tps_tracker.calculate_tps(30);
 
     let color_tps = |tps: f64| -> &'static str {
         if tps < 15.0 {
diff --git a/crates/proxy-core/src/net/packet_builder.rs b/crates/proxy-core/src/net/packet_builder.rs
index 768dfc7..77dd9bc 100644
--- a/crates/proxy-core/src/net/packet_builder.rs
+++ b/crates/proxy-core/src/net/packet_builder.rs
@@ -227,9 +227,9 @@ pub fn build_block_update_packet(x: i32, y: i32, z: i32, block_state_id: u32, pr
             // 1.7: packet 0x23 — Block Change
             // Fields: x(i32) y(u8) z(i32) block_type(VarInt) block_metadata(u8)
             VarInt(0x23_i32).encode(&mut payload).unwrap();
-            (x as i32).encode(&mut payload).unwrap();
+            x.encode(&mut payload).unwrap();
             (y as u8).encode(&mut payload).unwrap();
-            (z as i32).encode(&mut payload).unwrap();
+            z.encode(&mut payload).unwrap();
             // block_state_id encodes as type<<4|meta; pass as-is capped at VarInt
             VarInt(block_state_id as i32).encode(&mut payload).unwrap();
             0u8.encode(&mut payload).unwrap(); // metadata
diff --git a/crates/proxy-core/src/net/relay.rs b/crates/proxy-core/src/net/relay.rs
index c6573be..971265a 100644
--- a/crates/proxy-core/src/net/relay.rs
+++ b/crates/proxy-core/src/net/relay.rs
@@ -4,7 +4,6 @@ use std::sync::Arc;
 
 use kojacoord_protocol::{
     codec::Encode,
-    registry::{Direction, PacketRegistry, ProtocolState},
     types::VarInt,
     Decode,
 };
@@ -222,7 +221,7 @@ impl PacketRelay {
 
                 state_s2c.metrics.record_packet(payload.len());
 
-                state_s2c.tps_tracker.record_packet().await;
+                state_s2c.tps_tracker.record_packet();
 
                 let mut cur = payload.clone();
 
@@ -235,19 +234,12 @@ impl PacketRelay {
                     }
                 };
 
-                let registry = PacketRegistry::default();
-                let pkt_name = registry.get_name_from_id(proto, ProtocolState::Play, Direction::Clientbound, pkt_id)
-                    .unwrap_or("Unknown");
-                let session = session_s2c.read().await;
-                tracing::debug!(
+                tracing::trace!(
                     direction = "S→C",
                     packet_id = pkt_id,
-                    packet_name = pkt_name,
                     protocol = proto,
-                    player = %session.username,
                     "packet"
                 );
-                drop(session);
 
                 // ─── BlockUpdate intercept (S→C) ──────────────────────────
                 // When the server reveals what a block actually is at a
@@ -450,25 +442,12 @@ impl PacketRelay {
                         },
                     };
 
-                    let registry = PacketRegistry::default();
-                    let pkt_name = registry
-                        .get_name_from_id(
-                            proto,
-                            ProtocolState::Play,
-                            Direction::Serverbound,
-                            pkt_id,
-                        )
-                        .unwrap_or("Unknown");
-                    let session = session_c2s.read().await;
-                    tracing::debug!(
+                    tracing::trace!(
                         direction = "C→S",
                         packet_id = pkt_id,
-                        packet_name = pkt_name,
                         protocol = proto,
-                        player = %session.username,
                         "packet"
                     );
-                    drop(session);
 
                     state_c2s.metrics.record_packet(payload.len());
 
diff --git a/crates/proxy-core/src/proxy.rs b/crates/proxy-core/src/proxy.rs
index 694dcf1..7c7727e 100644
--- a/crates/proxy-core/src/proxy.rs
+++ b/crates/proxy-core/src/proxy.rs
@@ -1,9 +1,9 @@
 use bytes::Bytes;
 use dashmap::DashMap;
 use std::sync::Arc;
-use std::time::{Duration, Instant};
+use std::time::Instant;
 use tokio::net::TcpListener;
-use tokio::sync::{mpsc, RwLock, Semaphore};
+use tokio::sync::{mpsc, Semaphore};
 use uuid::Uuid;
 
 use crate::permissions::RoleRegistry;
@@ -23,31 +23,71 @@ use crate::server::ServerRegistry;
 use crate::server_management::ServerManagementServer;
 use crate::session::SharedSession;
 
-#[derive(Clone)]
+/// Lock-free TPS tracker using a fixed-size ring buffer of atomic timestamps.
+///
+/// The old design used `RwLock<Vec<Instant>>` with a write lock on every packet,
+/// followed by a linear retain(). This caused severe contention in the relay hot
+/// path — the root cause of the TPS degradation reported in production.
+///
+/// This replacement uses a ring buffer of `AtomicU64` slots holding microsecond
+/// offsets from a fixed epoch. `record_packet()` is a single `fetch_add` + store
+/// — zero locks, zero allocations, O(1). `calculate_tps()` does a lock-free scan
+/// of the ring and counts entries within the requested window.
 pub struct TpsTracker {
-    packet_timestamps: Arc<RwLock<Vec<Instant>>>,
+    /// Ring buffer of timestamps as microsecond offsets from `epoch`.
+    slots: Box<[std::sync::atomic::AtomicU64]>,
+    /// Write cursor — monotonically increasing, modulo `slots.len()`.
+    cursor: std::sync::atomic::AtomicUsize,
+    /// Fixed reference point for converting `Instant` → u64.
+    epoch: Instant,
 }
 
+/// Sentinel value meaning "slot is empty / not yet written".
+const EMPTY_SLOT: u64 = 0;
+
+/// Ring buffer capacity. 32768 slots ≈ enough for 30s at 1000 pkt/s.
+const RING_SIZE: usize = 32768;
+
 impl TpsTracker {
     pub fn new() -> Self {
+        let slots: Vec<std::sync::atomic::AtomicU64> = (0..RING_SIZE)
+            .map(|_| std::sync::atomic::AtomicU64::new(EMPTY_SLOT))
+            .collect();
         Self {
-            packet_timestamps: Arc::new(RwLock::new(Vec::new())),
+            slots: slots.into_boxed_slice(),
+            cursor: std::sync::atomic::AtomicUsize::new(0),
+            epoch: Instant::now(),
         }
     }
 
-    pub async fn record_packet(&self) {
-        let mut timestamps = self.packet_timestamps.write().await;
-        timestamps.push(Instant::now());
-
-        let cutoff = Instant::now() - Duration::from_secs(30);
-        timestamps.retain(|&t| t > cutoff);
+    /// Record a packet arrival. This is called on EVERY S→C packet in the relay
+    /// hot path, so it must be as cheap as possible — one atomic add, one atomic
+    /// store, no locks, no allocations.
+    #[inline]
+    pub fn record_packet(&self) {
+        let idx = self.cursor.fetch_add(1, std::sync::atomic::Ordering::Relaxed) % RING_SIZE;
+        let micros = self.epoch.elapsed().as_micros() as u64;
+        // Avoid writing the sentinel value (extremely unlikely, only at t=0).
+        let val = if micros == EMPTY_SLOT { 1 } else { micros };
+        self.slots[idx].store(val, std::sync::atomic::Ordering::Relaxed);
     }
 
-    pub async fn calculate_tps(&self, window_seconds: u64) -> f64 {
-        let timestamps = self.packet_timestamps.read().await;
-        let cutoff = Instant::now() - Duration::from_secs(window_seconds);
+    /// Count packets within the last `window_seconds` and compute TPS.
+    /// This is only called on `/tps` command — not on the hot path — so a full
+    /// scan of the ring is fine.
+    pub fn calculate_tps(&self, window_seconds: u64) -> f64 {
+        let now_micros = self.epoch.elapsed().as_micros() as u64;
+        let window_micros = window_seconds * 1_000_000;
+        let cutoff = now_micros.saturating_sub(window_micros);
+
+        let mut count = 0u64;
+        for slot in self.slots.iter() {
+            let val = slot.load(std::sync::atomic::Ordering::Relaxed);
+            if val != EMPTY_SLOT && val >= cutoff {
+                count += 1;
+            }
+        }
 
-        let count = timestamps.iter().filter(|&&t| t > cutoff).count();
         if count == 0 {
             return 20.0;
         }
diff --git a/patches/0001-feat-proxy-hardening-graceful-shutdown-handshake-kee.patch b/patches/0001-feat-proxy-hardening-graceful-shutdown-handshake-kee.patch
new file mode 100644
index 0000000..7801369
--- /dev/null
+++ b/patches/0001-feat-proxy-hardening-graceful-shutdown-handshake-kee.patch
@@ -0,0 +1,243 @@
+From 72bf21649475617311e6cc439b6f1468fdb1a0c9 Mon Sep 17 00:00:00 2001
+From: Kubmichna <jmichna64@gmail.com>
+Date: Fri, 5 Jun 2026 21:16:39 +0200
+Subject: [PATCH] feat: proxy hardening (graceful shutdown, handshake/keepalive
+ timeouts, health probes)
+
+---
+ crates/config/src/lib.rs                |  9 +++
+ crates/proxy-core/src/net/connection.rs | 25 +++++--
+ crates/proxy-core/src/net/relay.rs      | 18 +++++-
+ crates/proxy-core/src/proxy.rs          | 86 ++++++++++++++++++++++++-
+ src/main.rs                             |  8 ++-
+ 5 files changed, 138 insertions(+), 8 deletions(-)
+
+diff --git a/crates/config/src/lib.rs b/crates/config/src/lib.rs
+index c37fd2b..aa0c4b4 100644
+--- a/crates/config/src/lib.rs
++++ b/crates/config/src/lib.rs
+@@ -81,6 +81,11 @@ pub struct ProxySection {
+ 
+     #[serde(default = "default_auth_url")]
+     pub auth_url: String,
++
++    /// Maximum time (in ms) to wait for a client to send the initial handshake
++    /// packet after connecting. Protects against slow-loris attacks. Default: 5000.
++    #[serde(default = "default_handshake_timeout")]
++    pub handshake_timeout_ms: u64,
+ }
+ 
+ impl Default for ProxySection {
+@@ -100,6 +105,7 @@ impl Default for ProxySection {
+             server_id: generate_server_id(),
+             eula_accepted: false,
+             auth_url: default_auth_url(),
++            handshake_timeout_ms: default_handshake_timeout(),
+         }
+     }
+ }
+@@ -406,6 +412,9 @@ fn default_auth_url() -> String {
+ fn default_metrics_bind() -> String {
+     "127.0.0.1:9090".into()
+ }
++fn default_handshake_timeout() -> u64 {
++    5000
++}
+ 
+ fn generate_server_id() -> String {
+     uuid::Uuid::new_v4().to_string()
+diff --git a/crates/proxy-core/src/net/connection.rs b/crates/proxy-core/src/net/connection.rs
+index 2b3e873..6686a29 100644
+--- a/crates/proxy-core/src/net/connection.rs
++++ b/crates/proxy-core/src/net/connection.rs
+@@ -287,12 +287,27 @@ impl ClientConnection {
+ 
+         self.state.metrics.record_connection();
+ 
+-        let handshake = self
+-            .read_packet::<ServerboundHandshake>()
+-            .await
+-            .inspect_err(|_| {
++        let timeout_ms = self.state.config.proxy.handshake_timeout_ms;
++        let handshake = match tokio::time::timeout(
++            tokio::time::Duration::from_millis(timeout_ms),
++            self.read_packet::<ServerboundHandshake>(),
++        )
++        .await
++        {
++            Ok(Ok(hs)) => hs,
++            Ok(Err(e)) => {
+                 self.state.metrics.record_failed_connection();
+-            })?;
++                return Err(e);
++            },
++            Err(_elapsed) => {
++                self.state.metrics.record_failed_connection();
++                tracing::debug!(peer = %self.addr, timeout_ms, "handshake timed out");
++                return Err(ConnectionError::Io(std::io::Error::new(
++                    std::io::ErrorKind::TimedOut,
++                    "handshake timed out",
++                )));
++            },
++        };
+ 
+         self.protocol_version = handshake.protocol_version.0 as u32;
+ 
+diff --git a/crates/proxy-core/src/net/relay.rs b/crates/proxy-core/src/net/relay.rs
+index 0673875..7625a83 100644
+--- a/crates/proxy-core/src/net/relay.rs
++++ b/crates/proxy-core/src/net/relay.rs
+@@ -168,10 +168,26 @@ impl PacketRelay {
+                 if stopped_s2c_chk.load(Ordering::Acquire) {
+                     return Ok(());
+                 }
++                // Backend keepalive watchdog: if no S→C packet arrives within
++                // 30 seconds the backend is considered stalled (JVM GC pause,
++                // disk hang, etc.). Disconnect the player cleanly instead of
++                // leaving them in limbo with a leaked session slot.
+                 let payload = tokio::select! {
+                     biased;
+                     _ = stop_s2c_wait.notified() => return Ok(()),
+-                    r = read_packet(&mut *br_mut, backend_thresh) => r?,
++                    r = tokio::time::timeout(
++                        std::time::Duration::from_secs(30),
++                        read_packet(&mut *br_mut, backend_thresh),
++                    ) => match r {
++                        Ok(pkt) => pkt?,
++                        Err(_elapsed) => {
++                            tracing::warn!("relay: backend read timed out (30s) — disconnecting player");
++                            let msg = r#"{"text":"","extra":[{"text":"Connection Lost\n","bold":true,"color":"red"},{"text":"The backend server stopped responding.","color":"gray"}]}"#;
++                            let pkt = build_disconnect_packet(msg, proto);
++                            let _ = write_packet(&mut *cw_s2c.lock().await, &pkt, client_thresh).await;
++                            return Err(ConnectionError::Closed);
++                        }
++                    },
+                 };
+ 
+                 state_s2c.metrics.record_packet(payload.len());
+diff --git a/crates/proxy-core/src/proxy.rs b/crates/proxy-core/src/proxy.rs
+index cc44327..435b759 100644
+--- a/crates/proxy-core/src/proxy.rs
++++ b/crates/proxy-core/src/proxy.rs
+@@ -559,9 +559,69 @@ pub async fn accept_loop(state: Arc<ProxyState>) -> Result<(), anyhow::Error> {
+         }
+     });
+ 
++    // ── Backend Health Probing ──────────────────────────────────────────
++    // Periodically TCP-connect to every registered backend. On success set
++    // online=true; on failure set online=false. This lets the router skip
++    // downed servers immediately instead of making players wait for a
++    // connect timeout.
++    tokio::spawn({
++        let state = Arc::clone(&state);
++        async move {
++            let mut interval = tokio::time::interval(std::time::Duration::from_secs(10));
++            loop {
++                interval.tick().await;
++                let backends = state.server_registry.all();
++                for backend in &backends {
++                    let was_online = backend.is_online();
++                    let is_online = tokio::time::timeout(
++                        std::time::Duration::from_secs(3),
++                        tokio::net::TcpStream::connect(backend.address),
++                    )
++                    .await
++                    .map(|r| r.is_ok())
++                    .unwrap_or(false);
++
++                    backend
++                        .online
++                        .store(is_online, std::sync::atomic::Ordering::Relaxed);
++
++                    if was_online && !is_online {
++                        tracing::warn!(
++                            server = %backend.name,
++                            addr = %backend.address,
++                            "health probe: backend went OFFLINE"
++                        );
++                    } else if !was_online && is_online {
++                        tracing::info!(
++                            server = %backend.name,
++                            addr = %backend.address,
++                            "health probe: backend came ONLINE"
++                        );
++                    }
++                }
++            }
++        }
++    });
++
+     tracing::info!("Accepting incoming connections...");
++
++    // ── Graceful Shutdown ──────────────────────────────────────────────
++    // Accept connections until we receive Ctrl-C (SIGINT). On signal:
++    //   1. Stop accepting new connections
++    //   2. Broadcast a warning to all online players
++    //   3. Wait up to 5 seconds for relays to finish
++    //   4. Kick remaining players with a clean disconnect message
+     loop {
+-        let (stream, peer_addr) = listener.accept().await?;
++        let accept_result = tokio::select! {
++            biased;
++            _ = tokio::signal::ctrl_c() => {
++                tracing::info!("Received shutdown signal — beginning graceful drain...");
++                break;
++            }
++            r = listener.accept() => r,
++        };
++
++        let (stream, peer_addr) = accept_result?;
+         let peer = peer_addr.to_string();
+ 
+         // Connection-flood protection: reject (and temp-ban) IPs opening
+@@ -600,4 +660,28 @@ pub async fn accept_loop(state: Arc<ProxyState>) -> Result<(), anyhow::Error> {
+             state.metrics.record_disconnection();
+         });
+     }
++
++    // ── Drain phase ────────────────────────────────────────────────────
++    let shutdown_msg = r#"{"text":"","extra":[{"text":"\n\n","color":"white"},{"text":"Kojacoord Network\n","bold":true,"color":"gold"},{"text":"─────────────────────\n","color":"gray"},{"text":"Server Shutting Down\n\n","bold":true,"color":"red"},{"text":"The proxy is restarting. Please reconnect in a moment.\n","color":"white"},{"text":"─────────────────────\n","color":"gray"},{"text":"discord.gg/yourserver","color":"aqua","underlined":true}]}"#;
++    state.broadcast_system_message(shutdown_msg).await;
++    tracing::info!("Shutdown warning broadcast — draining for 5 seconds...");
++
++    tokio::time::sleep(std::time::Duration::from_secs(5)).await;
++
++    // Kick any remaining players
++    let remaining: Vec<uuid::Uuid> = {
++        let sessions = state.sessions.read().await;
++        sessions.keys().copied().collect()
++    };
++    if !remaining.is_empty() {
++        tracing::info!(count = remaining.len(), "Kicking remaining players...");
++        for uuid in &remaining {
++            state.kick_player(uuid, shutdown_msg).await;
++        }
++        // Give kick packets a moment to flush
++        tokio::time::sleep(std::time::Duration::from_millis(500)).await;
++    }
++
++    tracing::info!("Graceful shutdown complete.");
++    Ok(())
+ }
+diff --git a/src/main.rs b/src/main.rs
+index 55b2af1..c3f0d08 100644
+--- a/src/main.rs
++++ b/src/main.rs
+@@ -142,7 +142,13 @@ async fn main() -> anyhow::Result<()> {
+         }
+     });
+ 
+-    kojacoord_proxy_core::proxy::accept_loop(state).await
++    if let Err(e) = kojacoord_proxy_core::proxy::accept_loop(state).await {
++        tracing::error!("Proxy loop ended with error: {:?}", e);
++        std::process::exit(1);
++    }
++
++    tracing::info!("Process exiting.");
++    std::process::exit(0);
+ }
+ 
+ fn save_config(config: &kojacoord_config::ProxyConfig, path: &str) -> anyhow::Result<()> {
+-- 
+2.52.0.windows.1
+
-- 
2.52.0.windows.1

